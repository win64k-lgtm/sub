[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_pgsql", "php82Extensions.pgsql", "php82Extensions.mbstring"]

[phases.install]
cmds = ["echo 'PHP dependencies ready'"]

[start]
cmd = "cd subway-pay-main && php -S 0.0.0.0:$PORT"
